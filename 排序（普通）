// tools_sort_normal
//工具_排序_基础冒泡排序
//此示例用于对int类型的数组进行排序。
//这里展示对于一组整形数组的排序

//传入数组int arr []和元素个数int len
//传出无

void bubbleSort（int arr []，int len） 
{
    int temp;
    int i，j;
    for（i = 0; i <len-1; i ++）//从左至右排好升序或降序
        for（j = 1; j <len-i; j ++）//定某一位第i为的值
        {
            如果（ARR [I] <ARR [I + J]）
            {
                temp = arr [i + j];
                arr [i + j] = arr [i];
                arr [i] = temp;
            }
        }
}
